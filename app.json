{
  "name": "Informasi Unsri REST API",
  "description": "REST API untuk aplikasi Informasi Unsri",
  "repository": "https://github.com/DSC-UNSRI/cs-api",
  "logo": "https://avatars.githubusercontent.com/u/74489745",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "addons": [
    "heroku-postgresql:hobby-dev",
    "heroku-redis:hobby-dev"
  ],
  "env": {
    "ACCESS_TOKEN_KEY": {
      "description": "A secret key for verifying the integrity of signed Access Token.",
      "generator": "secret"
    },
    "REFRESH_TOKEN_KEY": {
      "description": "A secret key for verifying the integrity of signed Refresh Token.",
      "generator": "secret"
    },
    "OWNER_TOKEN_KEY": {
      "description": "A secret key for verifying the integrity of signed Owner Token.",
      "generator": "secret"
    },
    "FIREBASE_ADMIN_SDK_KEY": {
      "description": "A secret key for verifying the integrity of Firebase Admin SDK. can be found in, Project Settings > Service Accounts > Generate New Private Key > Paste the file content here",
      "required": true
    }
  },
  "scripts": {
    "postdeploy": "npm run migrate up"
  }
}